# Linux简介

- #### Linux是一个基于Posix和Uinx的类Unix的操作系统

- #### Posix是标准化Uinx的一个标准为了提高兼容性和应用程序的可移植性

# 常用指令

## 文件与目录操作

- ### ls：列出目录及文件名

  ```
  [root@www ~]# ls [-aAdfFhilnrRSt] 目录名称
  [root@www ~]# ls [--color={never,auto,always}] 目录名称
  [root@www ~]# ls [--full-time] 目录名称
  ```

  - #### 指令前面的是系统显示的信息

    - root：是用户名
    - www：是主机名
    - ~：是当前工作目录的最后一级名称，此处~代表家目录
    - #：表示当前用户是root用户，$表示当前用户是普通用户

  - #### 后面中括号里参数的作用是以什么样的形式打开目录及文件

    - -a ：显示所有文件，包括以  .  开头的隐藏文件（比如  .bashrc 、 .git ）。
    - -A ：显示所有文件，但不包含  . （当前目录）和  .. （上级目录）这两个特殊隐藏目录。
    - -d ：仅显示目录本身的信息，而不是列出目录里的文件。
    - -f ：不排序，直接列出文件；同时隐含  -a  效果，会显示所有文件。
    - -F ：在文件名后加上符号标记文件类型，比如目录加  / 、可执行文件加  * 。
    - -h ：将文件大小以人类易读的格式显示（如  1K 、 2M ），通常和  -l  一起用。
    - -i ：显示每个文件的 inode 编号（文件在文件系统中的唯一标识）。
    - -l ：以长格式显示，包含权限、所有者、大小、修改时间等详细信息。
    - -n ：和  -l  类似，但用 UID（用户ID）和 GID（组ID）代替用户名和组名。
    - -R ：递归显示所有子目录的内容。
    - -S ：按文件大小排序，最大的文件显示在最前面。
    - -t ：按修改时间排序，最新修改的文件显示在最前面

  - #### 第一行中括号中的是叠加的短参数，每个字母都代表着一定的操作，短参数叠加只需要加一个-在前面即可，当然每一个短参数都加-也行

  - #### 第二行和第三行都是长参数，需要加两个-在前面，长参数也可以叠加，但是叠加的每个长参数都要加--，不能效仿短参数

  - #### 真实的代码长这样`` ls -al ``，这个代码是以长格式显示默认目录的全部文件，包括隐藏文件

    - #### 目录名称的位置也有很多情况

      - 加绝对路径
      - 加相对路径
      - 什么都不加：打开当前目录
      - 加~：打开家目录(每个用户专属的个人目录，要根据用户类型来判断，比如root用户的家目录就是/root，普通用户的家目录是在/home目录下以用户名民命，如用户名为w，则家目录就为/home/w)

- ### cd：切换目录

  ```
  cd [相对路径或绝对路径]
  ```

  - #### #使用绝对路径切换到 runoob 目录

    ```
    [root@www ~]# cd /root/runoob/
    ```

    此代码在末尾加了一个/，是为了辨别同名目录和文件，若runoob目录下有一个文件也叫runoob，同时未在末尾加上/就会编译出错。是否添加/在语法上没有问题，但有同名目录与文件就会编译出错

  - #### #使用相对路径切换到 runoob 目录

    ```
    [root@www ~]# cd ./runoob/
    ```

  - #### 表示回到自己的家目录，即是回到/root目录

    ```
    [root@www runoob]# cd ~
    ```

  - #### 表示去到目前的上一级目录，亦即是 /root 的上一级目录的意思

    ```
    [root@www ~]# cd ..
    ```

    - 回到的是/(根目录)，前面的信息会变成[root@www /]#

  - #### 表示回到上一个目录

    ```
    [root@www ~]# cd 
    ```

- ### pwd：显示目前所在目录

  ```
  [root@www ~]# pwd [-P]
  ```

  - #### 显示出目录路径，但有可能是链接路径

    ```
    pwd
    ```

    - 链接储存目标目录的路径，就相当于c语言中的指针

  - #### 显示出实际工作路径，不会是链接路径

    ```
    pwd -P(大写)
    ```

- ### mkdir：创建新目录

  ```
  mkdir [-mp] 目录名称
  ```

  - #### 参数的含义

    - -m ：直接配置文件的权限

    - -p (小写)：帮助你直接将所需要的目录(包含上一级目录)递归创建起来

  - ####   创建多级目录
  
    ```
    [root@www ~]# cd /tmp
    [root@www tmp]# mkdir test    <==创建一名为 test 的新目录
    [root@www tmp]# mkdir test1/test2/test3/test4
    mkdir: cannot create directory `test1/test2/test3/test4': 
    No such file or directory       <== 没办法直接创建此目录啊！
    [root@www tmp]# mkdir -p test1/test2/test3/test4
    ```
  
  - ####  创建目录且配置目录权限 
  
    ```
    [root@www tmp]# mkdir -m 711 test2
    [root@www tmp]# ls -l
    drwxr-xr-x  3 root  root 4096 Jul 18 12:50 test
    drwxr-xr-x  3 root  root 4096 Jul 18 12:53 test1
    drwx--x--x  2 root  root 4096 Jul 18 12:54 test2
    ```
  
    - ##### 711的意思
  
      - r：读  	数字：4
      
      - w：写         数字：2
      
      - x：执行      数字：1
      
      - 所以此处的711数字代表的是权限
    - #####  第三行代码的意思
    
      - d：代表这是一个目录
      - rwx：所有者(root)的权限
      - r-x：所有组(root)的权限(所有组就是一个小组里所有的成员)
      - r-x：其他用户的权限
      - 第一个root是所有者的用户类型
      - 第二个root是所有组的用户类型
      - 4096是目录在文件系统中占用的默认大小，4096字节

- ### rm：移除文件或目录

  ```
   rm [-fir] 文件或目录
  ```

  - -f ：就是 force 的意思，忽略不存在的文件，不会出现警告信息；
  - -i ：互动模式，在删除前会询问使用者是否动作
  - -r ：递归删除啊！最常用在目录的删除了！这是非常危险的选项！！！

- ### touch：创建新的文件

  ```
  touch [-mt] 文件名称
  ```

  - #### 在当前目录创建新文件

    ```
    touch test.txt
    ```

  - #### 同时创建多个新文件

    ```
    touch a.txt b.log c.sh
    ```

  - #### 在指定目录创建新文件

    ```
    touch /root/tmp/info.txt
    ```

    - 在/root/tmp目录下创建info.txt

  - #### 更新文件的最后修改时间

    ```
    touch -m test.txt
    ```

    - 若文件已存在只会改变文件最后修改时间（默认同时修改访问时间+修改事件）不会改变文件内容

  - #### 改变文件修改时间

    ```
    touch -t 202601291800 test.txt  # 把test.txt时间戳改为2026-01-29 18:00
    ```

    - 格式为YYYYMMDDhhmm

- ### cat：从第一行显示文件内容

## 权限与执行

- ### chmod：改变文件或目录的权限

  ```
  chmod [-R(大写)] 755 文件或目录
  ```

  - #### 规则同mkdir

- ### sudo：让普通用户临时拥有root用户的权限

  ```
  sudo 指令
  ```

## 网络与端口

- ### ping：用于检测与另一个主机之间的网络连接，通过向目标主机发送数据包并等待回应，可以测量网络响应时间和丢包率，执行 ping 指令会使用 ICMP（Internet Control Message Protocol） 传输协议，发出要求回应的信息，若远端主机的网络功能没有问题，就会回应该信息，因而得知该主机运作正常

```
ping [选项] [目标主机/IP地址]
```

- `-c`：指定发送的数据包数量，例如 表示发送 4 个数据包。`-c 4`
- `-i`：指定每次发送数据包的间隔时间（秒），例如 表示每 0.5 秒发送一次。`-i 0.5`
- `-w`：设置发送数据包的等待时间上限，超出该时间后自动停止，例如 表示等待 5 秒。`-w 5`
- `-s`：指定每个数据包的大小（字节），默认是 56 字节。
- `-t`：设置数据包的生存时间（TTL），指定路由跳数。
- `-q`：安静模式，只显示开始和结束的统计数据，不显示每个数据包的详细信息。
- `-f`：疯狂模式，快速发送数据包，用于测试网络承载能力，需谨慎使用。
- `-l`：指定一次发送的数据包数量，通常用于负载测试。
- `-v`：显示详细输出信息，用于调试。
- `-4`：强制使用 IPv4 协议。
- `-6`：强制使用 IPv6 协议。

- ### netstat ：用于显示网络状态，利用 netstat 指令可让你得知整个 Linux 系统的网络情况

```
netstat [-acCeFghilMnNoprstuvVwx][-A<网络类型>][--ip]
```

- -a或--all 显示所有连线中的Socket。
- -A<网络类型>或--<网络类型> 列出该网络类型连线中的相关地址。
- -c或--continuous 持续列出网络状态。
- -C或--cache 显示路由器配置的快取信息。
- -e或--extend 显示网络其他相关信息。
- -F或--fib 显示路由缓存。
- -g或--groups显示多重广播功能群组组员名单。
- -h或--help 在线帮助。
- -i或--interfaces 显示网络界面信息表单。
- -l或--listening 显示监控中的服务器的Socket。
- -M或--masquerade 显示伪装的网络连线。
- -n或--numeric 直接使用IP地址，而不通过域名服务器。
- -N或--netlink或--symbolic 显示网络硬件外围设备的符号连接名称。
- -o或--timers 显示计时器。
- -p或--programs 显示正在使用Socket的程序识别码和程序名称。
- -r或--route 显示Routing Table。
- -s或--statistics 显示网络工作信息统计表。
- -t或--tcp 显示TCP传输协议的连线状况。
- -u或--udp 显示UDP传输协议的连线状况。
- -v或--verbose 显示指令执行过程。
- -V或--version 显示版本信息。
- -w或--raw 显示RAW传输协议的连线状况。
- -x或--unix 此参数的效果和指定“-A unix”参数相同。
- --ip或--inet 此参数的效果和指定“-A inet”参数相同。



## 进程与服务

- ### ps：命令用于显示当前进程的状态，类似于 windows 的任务管理器

- ### systemctl ：是 Linux 系统中用于控制 systemd 系统和服务管理器的命令行工具。作为现代 Linux 发行版的核心组件，它取代了传统的 init 系统和 service 命令

## 调试与日志查看

- ### crul：用于在Linux/Unix系统中传输数据，支持多方协议

  - ### curl 的主要特点

    1. **多协议支持**：支持几乎所有主流网络协议
    2. **无界面操作**：纯命令行工具，适合脚本和自动化任务
    3. **功能丰富**：支持文件上传下载、表单提交、Cookie 处理等
    4. **跨平台**：在 Linux、macOS、Windows 上均可使用

  - ```
    curl [options] [URL...]
    ```

    - **options**：各种可选参数，用于控制crul行为
    - **URL**：要访问的一个或多个网址

  - #### 基本请求控制

    | ``-X`` | 指定 HTTP 方法                  | ``curl -X POST https://example.com``                         |
    | :----: | :------------------------------ | ------------------------------------------------------------ |
    | ``-d`` | **发送 POST 数据**              | ``curl -d "name=John" https://example.com``                  |
    | ``-G`` | **将 -d 数据作为 GET 参数发送** | ``curl -G -d "q=keyword" https://search.com``                |
    | ``-H`` | **添加请求头**                  | ``curl -H "Content-Type: application/json" https://api.com`` |

    - ##### -X一般配合非GET/POST方法使用，不然显得冗余，上述代码没有给服务器任何数据，是个空请求制作演示用

    - ##### 求头：是指``额外说明``,比如上述代码就是告诉服务器这次数据的格式是JSON类型的要按照这个类型来解析

  - #### 输出控制

    | `-o` | 将输出保存到文件           | `curl -o output.html https://example.com` |
    | ---- | -------------------------- | ----------------------------------------- |
    | `-O` | **使用远程文件名保存**     | `curl -O https://example.com/file.zip`    |
    | `-s` | **静默模式（不显示进度）** | `curl -s https://api.com/data.json`       |
    | `-v` | **显示详细通信过程**       | `curl -v https://example.com`             |

    - ###### 第一行是将得到的内容保存到output.html文件中

    - ###### 第二行则是将获取的数据以远程文件本来的名字保存到本地

  - #### 认证与安全

    | 选项       | 说明                  | 示例                                        |
    | :--------- | :-------------------- | :------------------------------------------ |
    | `-u`       | **用户名密码认证**    | `curl -u user:pass https://secure.com`      |
    | `-k`       | **忽略 SSL 证书验证** | `curl -k https://self-signed.com`           |
    | `--cacert` | **指定 CA 证书**      | `curl --cacert cert.pem https://secure.com` |

    - **第一行：输入用户名和密码时，替换掉user和pass即可**

  - ### 其他实用选项

    | 选项           | 说明               | 示例                                           |
    | :------------- | :----------------- | :--------------------------------------------- |
    | `-L`           | **跟随重定向**     | `curl -L https://short.url`                    |
    | `-I`           | **只获取头部信息** | `curl -I https://example.com`                  |
    | `--limit-rate` | **限制传输速度**   | `curl --limit-rate 100K https://largefile.com` |

    - **第一行：简单来说就是如果访问的服务器返回“重定向”响应(即是需要跳转到新的网址)时，会直接跳转**
    - **第二行：只返回响应头，用于快速检查状态码，服务器信息等**

- ### tail：用于查看文件内容

  ```
  tail [参数] [文件] 
  ```

  - ##### ``-f``：循环读取，直到按下CTRL+C停止

  - ##### ``-n``[行数]：显示文件尾部的n行内容

  - ##### ``-c``[数目]：显示最后n个字节数

  - ##### 要显示 notes.log 文件的最后 10 行，请输入以下命令：

    ```
    tail notes.log         # 默认显示最后 10 行
    ```

  - ##### 要跟踪名为 notes.log 的文件的增长情况，请输入以下命令：

    ```
    tail -f notes.log
    ```

    - ##### 按下CTRL+C会停止循环
